# Local development only.
# Pace & Plates stores the actual runtime key in Keychain after first launch.
# If you run from Xcode, expose this value as the `OPENROUTER_API_KEY`
# environment variable for the app process, or paste the key into Settings.

OPENROUTER_API_KEY=
